# v2 rebuild, parked

The kinetic / WebGL rebuild. Parked here (not deleted) when the old design was
restored, so it can be brought back if wanted.

Contains the full design system, the persistent Three.js point field, the GSAP
scroll primitives, and the design contract that governs them.
